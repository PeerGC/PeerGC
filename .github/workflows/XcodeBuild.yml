name: XcodeBuild

on:
  push:

jobs:
  XcodeBuild:

    runs-on: macOS-latest
    
    steps:
    - uses: actions/checkout@master
    - name: Start xcodebuild test
      run: xcodebuild clean test -project PeerGC.xcworkspace -scheme PeerGC -destination "platform=iOS Simulator,name=iPhone 11 Pro"
